# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.pyo

# Virtual environments
.venv/
venv/
env/

# Tests & coverage
.pytest_cache/
.coverage
htmlcov/

# Alembic generated SQL scripts (not needed in image)
migrations/versions/*.sql

# Data files (mounted as a volume or seeded separately)
data/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Secrets (never bake into the image)
.env
.env.*
